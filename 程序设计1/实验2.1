#include <stdio.h>
#define PI 3.14
int main()
    {
	double r,h,s,v;
	printf("请输入圆柱体的底面半径：\n");	
	scanf("%lf",&r);
    printf("请输入圆柱体的高：\n");
    scanf("%lf",&h);
	printf("圆柱体的体积为：%lf\n",v=PI*r*r);
	printf("圆柱体的表面积为：%lf\n",s=2*PI*r*r+2*PI*r*h);
	return 0;

	}
    
